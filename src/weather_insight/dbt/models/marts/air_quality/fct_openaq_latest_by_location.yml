version: 2

models:
  - name: fct_openaq_latest_by_location
    description: Latest validated OpenAQ measurements by location and sensor.
    tests:
      - elementary.volume_anomalies
    columns:
      - name: measurement_value
        tests:
          - accepted_range:
              min_value: 0
